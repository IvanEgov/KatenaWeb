@model TextField
@{
	ViewBag.Title = Model.Title;
	ViewBag.Text = Model.Text;
	ViewBag.TitleImagePath = Model.TitleImagePath;
}

@* <div>
	@Html.Raw(Model.Text)
	@if(ViewBag.TitleImagePath != null)
	{
		<div id="picture" style="margin-left:650px;">
			<div>
				<img class="img-block" src="~/images/@ViewBag.TitleImagePath" />
			</div>
		</div>
	}
	<div style="display:inline-block;">
		<button class="button" id="homeButton"><a asp-area="" asp-controller="Home" asp-action="Index">Вернуться на главную</a></button>
	</div>
</div> *@
<div id="wrapper">
	<section id="intro" class="wrapper style1 fullscreen fade-up">
		<div class="inner">
			<h1 style="font-family:'Firenight-Regular'">@Html.Raw(Model.Title)</h1>
			<div id="picture" style="margin-left:650px;">
				<div>
					<img class="img-block" src="~/images/@ViewBag.TitleImagePath" />
				</div>
			</div>
			<p>
				<h3 style="font-family:'Firenight-Regular'; font-size:large;">@Html.Raw(Model.Text)</h3>
			</p>
			<ul class="actions">
				<li><a asp-area="" asp-controller="Home" asp-action="Index" class="button scrolly">Вернуться на главную</a></li>
				<li><a asp-area="" asp-controller="Tests" asp-action="Index" asp-route-id="" class="button scrolly">Пройти тест</a></li>
			</ul>
		</div>
	</section>
<div/>