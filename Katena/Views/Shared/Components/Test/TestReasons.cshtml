@using Katena.Domain
@inject DataManager datamanager;

@{ViewBag.picture += ".png";}
<div class="test">
	<div class="question-text">
		<h1 class="question-symbol">"</h1>
		<h2>@ViewBag.question.Name</h2>
	</div>
	<div class="line-wrapper">
		<span class="line"></span>
	</div>
	<div class="reson-image-and-motive">
		<div class="reson-image">
			<img class="question-image-circle" src="~/images/@ViewBag.picture" alt="" data-position="center" />
			<h2 class="progerss-text">Ситуация @(ViewBag.index + 1)/@ViewBag.pack.QuestionsIds.Count</h2>
		</div>
		<div class="motives">
			@* <div class="takeAction"> *@
			<h3 class="taken-action">Вы выбрали действие: </h3>
			<div class="button-question">
				<input class="button-action-post" type="submit" value="@ViewBag.answer.Action" disabled="disabled">
			</div>
			@* </div> *@
			<h3 class="taken-action">Каков ваш мотив?</h3>
			<div class="resons">
				@foreach (Guid id in ViewBag.answer.reasonsId)
				{
					ReasonBase reason = datamanager.Reasons.GetReasonById(id);
					<form style="display: inline-block;" asp-controller="Tests" asp-action="NextQuestion">
						<input type="hidden" name="pack" value="@ViewBag.pack.Id">
						<input type="hidden" name="index" value="@ViewBag.index">
						<input type="hidden" name="question" value="@ViewBag.question.Id">
						<input type="hidden" name="answer" value="@ViewBag.answer.Id">
						<input type="hidden" name="reason" value="@reason.Id">
						<input type="hidden" name="gender" value="@ViewBag.gender">
						<input type="hidden" name="checkReason" value="false">
						<div class="button-question">
							<input type="submit" class="button-action" value="@reason.Reason">
						</div>
					</form>
				}
			</div>
		</div>
	</div>
</div>

