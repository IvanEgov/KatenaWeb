@using Katena.Domain
@model QuestionBase
@{
	ViewBag.Title = Model.Name;
}
@inject DataManager datamanager;

<script>
	document.getElementById("startButton").addEventListener(
		"click",
		function () {
			document.getElementById("question").hidden = true;
			document.getElementById("start").hidden = false;
		},
		false,
	);
</script>

@*<div id="sidebar">
	<div id="test">
		<h4>@Model.Name</h4>
		<button class="botton" id="startBotton">Начать тест</button>
		<form asp-controller="Test" asp-action="Index" method="post">
		<input type="hidden" name ="test" value="Model">
		<button><a href="#" onclick="document.getElementById('form-@Model.Id').submit();">Начать тест</a></button>
		<bottom><a asp-controller="" asp-action="" method="post">Начать тест</a></bottom>
		</form>
	</div> *@

	<div id="question" hidden>
		@foreach (Guid answerId in Model.AnswerId)
		{
			AnswersBase answer = datamanager.Answers.GetAnswerById(answerId);
			<button><h1>@answer.Action</h1></button>
			<div id="start">
				@await Html.PartialAsync("answerPartial", answer)
			</div>
			
		}
	</div>
</div>